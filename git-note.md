# Git 学习笔记
## 基础命令
1. git init :初始化本地仓库
2. git add 文件名 ：把文件加入暂存区
3. git commit -m "提交信息"： 暂存区内容提交到本地版本库
4. git status:查看仓库状态
5. git log：查看提交历史
6. git pull:拉取远程仓库更新到本地。
7. git push：把本地提交推送到远程Github仓库。
8. touch +文件名：创建空白文件。
在之后的题目当中，一般先git status查看哪些文件被我修改了，然后git add把我想要上传的文件添加到暂存区，然后git
commit -m提交到本地仓库，最后再用git push推送到远程仓库。（在这个过程中我尝试了直接在远程仓库修改README文件，
直接push会报错，需要先git pull把你做的修改拉取到本地，然后在git push）


## 对Git的文件了解
 Git的文件状态分为三种：工作目录，暂存区，本地仓库。
 - 工作目录：实际操作文件的地方，包括查看，编辑，删除和创建文件。里面的文件可能有未跟踪，已修改这些状态。
 - 暂存区：即索引，临时存储区域，用于保存即将提交到本地仓库的修改。
 - 本地仓库：隐藏在.git目录中的数据库，用于存储项目的所有提交历史记录。每次提交更改时，Git会将暂存区的内容保存到本地仓库中。

## 一些小体验
1. 用touch或nano 创建文件后，用git status查看状态，会发现他们显示Untracked,也就是未跟踪，存于工作目录，但没有
纳入版本管理。
2. 用git add之后,git才会去追踪这个文件的修改。
3. 对已跟踪的文件更改后，用Git status会显示为已修改状态。
4. 用git add 会显示Staged。
5. git commit 会显示Committed。
